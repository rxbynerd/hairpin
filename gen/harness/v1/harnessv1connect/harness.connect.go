// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: harness/v1/harness.proto

package harnessv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/rxbynerd/hairpin/gen/harness/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HarnessServiceName is the fully-qualified name of the HarnessService service.
	HarnessServiceName = "stirrup.harness.v1.HarnessService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HarnessServiceRunTaskProcedure is the fully-qualified name of the HarnessService's RunTask RPC.
	HarnessServiceRunTaskProcedure = "/stirrup.harness.v1.HarnessService/RunTask"
)

// HarnessServiceClient is a client for the stirrup.harness.v1.HarnessService service.
type HarnessServiceClient interface {
	// RunTask opens a bidirectional stream. The control plane sends a
	// TaskAssignment as the first ControlEvent; the harness streams
	// HarnessEvents back as it works, and reads ControlEvents for user
	// responses, permission decisions, and cancellation signals.
	RunTask(context.Context) *connect.BidiStreamForClient[v1.HarnessEvent, v1.ControlEvent]
}

// NewHarnessServiceClient constructs a client for the stirrup.harness.v1.HarnessService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHarnessServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HarnessServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	harnessServiceMethods := v1.File_harness_v1_harness_proto.Services().ByName("HarnessService").Methods()
	return &harnessServiceClient{
		runTask: connect.NewClient[v1.HarnessEvent, v1.ControlEvent](
			httpClient,
			baseURL+HarnessServiceRunTaskProcedure,
			connect.WithSchema(harnessServiceMethods.ByName("RunTask")),
			connect.WithClientOptions(opts...),
		),
	}
}

// harnessServiceClient implements HarnessServiceClient.
type harnessServiceClient struct {
	runTask *connect.Client[v1.HarnessEvent, v1.ControlEvent]
}

// RunTask calls stirrup.harness.v1.HarnessService.RunTask.
func (c *harnessServiceClient) RunTask(ctx context.Context) *connect.BidiStreamForClient[v1.HarnessEvent, v1.ControlEvent] {
	return c.runTask.CallBidiStream(ctx)
}

// HarnessServiceHandler is an implementation of the stirrup.harness.v1.HarnessService service.
type HarnessServiceHandler interface {
	// RunTask opens a bidirectional stream. The control plane sends a
	// TaskAssignment as the first ControlEvent; the harness streams
	// HarnessEvents back as it works, and reads ControlEvents for user
	// responses, permission decisions, and cancellation signals.
	RunTask(context.Context, *connect.BidiStream[v1.HarnessEvent, v1.ControlEvent]) error
}

// NewHarnessServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHarnessServiceHandler(svc HarnessServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	harnessServiceMethods := v1.File_harness_v1_harness_proto.Services().ByName("HarnessService").Methods()
	harnessServiceRunTaskHandler := connect.NewBidiStreamHandler(
		HarnessServiceRunTaskProcedure,
		svc.RunTask,
		connect.WithSchema(harnessServiceMethods.ByName("RunTask")),
		connect.WithHandlerOptions(opts...),
	)
	return "/stirrup.harness.v1.HarnessService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HarnessServiceRunTaskProcedure:
			harnessServiceRunTaskHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHarnessServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHarnessServiceHandler struct{}

func (UnimplementedHarnessServiceHandler) RunTask(context.Context, *connect.BidiStream[v1.HarnessEvent, v1.ControlEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("stirrup.harness.v1.HarnessService.RunTask is not implemented"))
}
